name: play_m8
description: A new Flutter project.
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  go_router: ^14.6.2
  hive: ^2.2.3
  hive_flutter: ^1.1.0
  flutter_card_swiper: ^4.1.3
  lottie: ^3.1.2
  http: ^1.2.2

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
